# qifan 本地知识库

这是一个纯静态的个人知识库网页，用于集中浏览读书知识、饭局社交攻略、情绪沟通课程和《资治通鉴》解读等内容。

项目采用“知识首页 + 沉浸阅读器”双层结构：首页负责搜索、分类选择和继续阅读，阅读器采用当前分类文章列表与正文双栏。桌面端强调查找效率，手机端强调顺滑阅读，折叠屏和平板强调左右分工。

## 设计原则

- 结构清晰：首页承担发现和搜索；阅读器在宽屏采用当前分类文章列表与正文双栏，手机端采用列表与正文双状态。
- 色彩克制：整体控制在灰白背景、面板色、文字色和绿色品牌色四类颜色内，绿色只用于当前选中和必要操作。
- 字体稳定：中文优先使用系统字体，正文保持舒适行高，避免默认小灰字带来的廉价感。
- 留白统一：间距遵循 4 / 8 / 12 / 16 / 24 / 32 / 48 的体系，避免随意魔法数字。
- 动效克制：只保留 hover、选中态和手机端平滑滚动，不做炫技动画。

## 目录结构

- `index.html`：页面骨架，只保留结构和资源引用。
- `assets/css/styles.css`：样式入口文件，只负责引入各层 CSS。
- `assets/css/tokens.css`：颜色、字体、间距、圆角、阴影等设计令牌。
- `assets/css/base.css`：全局基础样式和滚动条样式。
- `assets/css/layout.css`：页面布局、三栏结构和阅读区约束。
- `assets/css/components.css`：搜索框、筛选按钮、卡片、标签、详情、表格等组件样式。
- `assets/css/responsive.css`：桌面、折叠屏/平板、手机、小手机的响应式规则。
- `assets/js/data.js`：生成后的知识库数据，向页面暴露 `window.KNOWLEDGE_DATA`。
- `assets/js/app.js`：搜索、筛选、卡片列表、详情渲染和手机端交互。

## 维护约定

- 新增或更新知识库内容时，先修改 Markdown 卡片，再运行 `scripts/build_knowledge_html.py` 重建 `assets/js/data.js`。
- 每篇卡片使用 1-2 个固定主题和 3-8 个具体标签；主题用于归类，标签用于人物、事件、场景、概念和方法检索。
- 调整颜色、间距、字号时，优先更新 `assets/css/tokens.css`。
- 调整桌面、折叠屏、手机端布局时，优先更新 `assets/css/responsive.css` 和 `assets/css/layout.css`。
- 新增筛选、排序、收藏等交互时，优先更新 `assets/js/app.js`。
- 不要把长数据、复杂样式或大段脚本重新写回 `index.html`。

## 版本更新记录

### v0.13.0 - 2026-07-16

- 将原先混合使用的 195 个主题整理为“主题 + 标签”两级分类法。
- 全库主题归一为 34 个稳定主题，具体人物、事件、地点、场景和概念保留为可搜索标签。
- 阅读页分开展示主题与标签，搜索同时覆盖两类字段。
- 左上角 `>|` 标识改为 `▦`，正文小标题前的 `>` 改为绿色细竖线。
- 修正网页生成器，只重建 `assets/js/data.js`，不再覆盖结构化 `index.html`。

### v0.12.0 - 2026-07-10

- 修复桌面阅读状态的页面级高度溢出，只保留左侧列表和正文两个必要滚动区。
- 滚动条默认隐藏，鼠标进入对应滚动区域后才以细线形式出现。
- 阅读器最大宽度收紧至约 1140px，双栏间距调整为 32px，正文宽度控制在约 740px。
- 主标题调整为 36px 和 700 字重，减少视觉压迫；正文顶部和首章节间距同步收紧。
- 加深元信息和未选中文章文字，提高辅助信息可读性。
- 当前文章选中态简化为绿色竖线与深色标题，移除浅绿背景叠加。
- 新增文章内容分层进入、列表轻微位移、返回按钮和搜索焦点等克制微动画。

### v0.11.0 - 2026-07-10

- 为首页进入阅读页增加短促的淡入与横向衔接动画。
- 阅读页调整为“当前分类文章列表 + 正文”的双栏结构。
- 删除苹果窗口式彩色圆点、路径顶栏、内容框边框和面板背景。
- 删除右侧本文目录，并将预计阅读时间并入文章元信息。
- 左侧只渲染当前文章所属分类，避免其他分类干扰连续阅读。

### v0.10.2 - 2026-07-10

- 删除首页品牌区域中的常驻搜索框。
- 点击右上角搜索图标后打开居中搜索浮层，可通过关闭按钮、遮罩或 Esc 关闭。
- 搜索浮层在桌面端和手机端使用对应尺寸，并保留 Enter 直接进入匹配内容的行为。

### v0.10.1 - 2026-07-10

- 删除首页 `KNOWLEDGE / 2026` 和 `EXPLORE / 01` 英文装饰信息。
- 将首页搜索框移入品牌首屏，保持与说明文字同宽并左对齐。
- 调整首屏阅读顺序为标题、说明、搜索、继续阅读，减少搜索框与分类区之间的视觉冲突。

### v0.10.0 - 2026-07-10

- 精简知识首页，删除“浏览全部内容”、黑色终端预览和最近内容区。
- 重排品牌首屏，将“知识分类”提升为首页主要内容标题。
- 顶部导航动态展示全部知识分类，点击分类恢复该分类上次阅读的文章和滚动位置。
- 新增分层入场动效、分类卡片错峰出现、搜索框聚焦反馈和按钮、导航、卡片的细微悬停动画。
- 动效遵循系统“减少动态效果”设置，避免影响易感用户和持续阅读。

### v0.9.0 - 2026-07-10

- 新增独立知识首页，包含品牌首屏、全局搜索、分类入口、最近内容和继续阅读。
- 阅读页调整为“左侧分类文章树 + 中央正文 + 右侧本文目录”的沉浸式阅读结构。
- 视觉语言调整为灰白背景、绿色品牌色和克制的终端式细节，正文宽度约 760px。
- 宽屏显示三栏，普通桌面与平板使用双栏，手机采用文章列表与沉浸正文两个独立状态。
- 增加最近阅读记录、本页目录定位、移动端目录浮层和分类折叠功能。

### v0.8.0 - 2026-07-10

- 细化平板和折叠屏端的压缩导航布局。
- 折叠屏压缩状态保留 220-280px 文章列表，便于继续切换文章。
- 平板压缩状态保留 168-220px 文章列表，同时进一步压低最左侧摘要栏权重。
- 平板压缩状态下收紧标题栏、卡片标题和间距，优先保障右侧阅读区。

### v0.7.0 - 2026-07-10

- 调整压缩导航的信息层级：最左侧窄信息栏降为最低视觉权重。
- 压缩状态下隐藏筛选栏，保留中间文章列表并压缩为“序号 + 标题”模式。
- 用户在压缩状态下可以继续切换文章，切换后保持压缩阅读状态不变。
- 阅读区继续作为主视觉区域，中间列表仅承担快速切换文章的辅助职责。

### v0.6.0 - 2026-07-10

- 将“专注阅读”从完全隐藏左侧改为“压缩导航”模式，避免阅读时失去定位感。
- 专注阅读时保留窄信息栏，显示卡片数、主题数、当前分类和当前文章序号。
- 桌面端保留弱化后的筛选栏，隐藏中间列表，扩大详情阅读区域。
- 平板和折叠屏保留窄信息栏并强化详情区，减少侧栏占用。

### v0.5.0 - 2026-07-10

- 新增桌面端、平板和折叠屏可用的“专注阅读”模式。
- 在详情吸顶标题区增加“专注阅读 / 展开列表”切换按钮。
- 专注阅读时隐藏左侧筛选栏和中间列表，只保留当前文章标题、元信息和正文，降低阅读干扰。
- 手机端继续使用原有阅读模式和“返回列表”，不额外展示专注阅读按钮。

### v0.4.0 - 2026-07-10

- 将详情区升级为阅读器体验：增加吸顶阅读标题、收窄正文宽度、强化段落行高和小标题节奏。
- 弱化左侧筛选栏和中间卡片列表的边框与阴影，降低对正文阅读的干扰。
- 优化折叠屏和平板布局：中等屏采用左侧导航/列表、右侧详情阅读的左右分工。
- 优化手机端阅读体验：点击卡片后进入阅读模式，只显示详情内容，并提供“返回列表”按钮。
- 补充阅读体验设计令牌：正文最大宽度、正文行高、段落间距等统一由 CSS 变量管理。

### v0.3.0 - 2026-07-10

- 按设计系统拆分 CSS：新增 `tokens.css`、`base.css`、`layout.css`、`components.css`、`responsive.css`。
- 强化多端观看体验：桌面端三栏、折叠屏/平板双栏、手机端单列。
- 优化卡片标签展示：列表最多显示 3 个标签，多余标签显示为 `+N`。
- 将 README 改为中文项目介绍和版本更新记录。

### v0.2.0 - 2026-07-10

- 将单文件页面拆分为 `index.html`、`assets/css/styles.css`、`assets/js/data.js`、`assets/js/app.js`。
- 保留纯静态部署方式，直接打开 `index.html` 即可使用。
- 将数据和应用逻辑封装到 `window.KNOWLEDGE_DATA` 和独立脚本中，减少全局变量污染。

### v0.1.0 - 2026-07-10

- 完成首版本地知识库页面。
- 支持分类筛选、主题筛选、关键词搜索、卡片列表和详情阅读。
- 基于视觉评审优化滚动条、卡片选中态、正文留白、色彩克制和手机端适配。
